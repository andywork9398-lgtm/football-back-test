# 馬會亞盤模型骨架（v0.1 · 細倉試水）

目標：每場輸出「有冇 edge」同建議注碼，唔係預測比分好看。

---

## 1. 輸出（每場必有）

| 欄位 | 意思 |
|------|------|
| `mu` | 預期淨勝球（主隊視角，含主場） |
| `sigma` | 淨勝球不確定性（起步可用 1.0–1.2） |
| `p_cover` | 打指定讓球盤（含走水規則）嘅覆蓋概率 |
| `fair_odds` | 1 / p_cover（未計抽水） |
| `edge_pct` | (p_cover × 馬會賠率 − 1) × 100 |
| `decision` | Pass / 0.5u / 1u |

**入場門檻（v0.1）**：`edge_pct ≥ 5` 先考慮；≥8 先上到 1u。未過門檻 = Pass。

---

## 2. 核心公式（簡化可落地）

### Step A · 預期入球
- `λ_home` = 主隊攻擊強度 × 客隊防守脆弱 × 主場因子
- `λ_away` = 客隊攻擊強度 × 主隊防守脆弱
- `mu = λ_home − λ_away`（再加減動機／輪換調整，單位：球）

強度起步可用：**近 N 場 xG for / xG against**（建議 N=6–10，權重近賽較高），聯賽內標準化。

### Step B · 淨勝球分布
假設淨勝球 ~ Normal(`mu`, `sigma²`)。  
（進階版可改 bivariate Poisson / Dixon-Coles；v0.1 用 Normal 夠快驗證流程。）

### Step C · 亞盤覆蓋概率（常見線）

以主讓 `h` 為例（h>0 主熱讓球；客熱則 h 為負，或改客隊視角，二選一保持一致）：

| 盤口類型 | 概約計算 |
|----------|----------|
| 平手 0 | P(主勝) + 0.5×P(和) |
| 平半 0.25 | 0.5×P(淨勝≥0 全贏區) + 0.5×P(走水／半贏區) … 按馬會「贏半／輸半」拆 |
| 半球 0.5 | P(淨勝 ≥ 1) |
| 半一 0.75 | 半注 0.5 + 半注 1.0 規則拆開平均 |
| 一球 1.0 | P(淨勝 ≥ 2) + 0.5×P(淨勝=1)（贏一球走） |

實作時：**用模擬 20k 次抽 Normal，再按馬會該線規則結算** 最穩，唔使死記每條半檔公式。

### Step D · Edge
`edge = p_cover × decimal_odds − 1`  
只打 `edge ≥ 0.05`；注碼跟已鎖規則（0.5–1u）。

---

## 3. 特徵（v0.1 最少集）

**必須**
1. 近況 xG for / against（主客分開更好）
2. 主場因子（聯賽平均主場 mu 加成，英超約 +0.25～0.35 球量級，用數據校準）
3. 休息日差（少休 −mu 或加大 sigma）
4. 明確傷停／輪換（中軸、主力前鋒：手動調 `mu` ±0.1～0.3）

**暫緩（有樣本再加）**
- 裁判嚴寬、天氣、旅行距離
- 市場開盤作先验（可做 shadow：模型 vs 開盤）

---

## 4. 工作流（每場 5 分鐘）

1. 抄馬會盤：線 `h`、主水、客水  
2. 填特徵 → 計 `mu` / `sigma`  
3. 模擬 → `p_cover`（主／客各算一次，揀有 edge 嗰面）  
4. `edge_pct` 過門檻？→ 寫 edge log（Pending）  
5. 開賽前記收盤 → CLV  
6. 結算 → 更新 hit rate / ROI（只計有下注；Pass 另計「錯過嘅正 edge」可選）

---

## 5. 驗證指標（細倉目標）

- **CLV+ 比例** > 50%（比純勝率優先）
- 樣本 ≥ 30 注先討論加倉
- 分聯賽看：邊個聯賽模型失效就降權或停

---

## 6. 已知限制

- Normal 近似低估 0-0 / 極端波膽；大熱讓波（≥1.5）誤差大 → 該類降注或 Pass
- 手動調傷停容易 overfit → 每次調整要寫入 notes
- 馬會抽水高 → **門檻 5% 唔可以再低**

---

## 7. 下一版（v0.2）候選

- 改 Dixon-Coles / xG 模擬入球
- 自動拉 xG（需數據源）
- 開盤作 Bayesian prior
